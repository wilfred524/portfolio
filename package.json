{
  "name": "portfolio",
  "private": true,
  "version": "0.1.0",
  "description": "Página de presentación — colección de diseños con frontend (web) y backend (api) separados",
  "workspaces": [
    "shared",
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,cyan \"npm run dev -w @portfolio/api\" \"npm run dev -w @portfolio/web\"",
    "build": "npm run build -w @portfolio/api && npm run build -w @portfolio/web",
    "build:web": "npm run build -w @portfolio/web"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
